---
import BaseLayout from '~/layouts/BaseLayout.astro';
import { SITE } from '~/config/site';
import { breadcrumbSchema } from '~/lib/schema';
import ImageSlot from '~/components/ImageSlot.astro';

const breadcrumb = breadcrumbSchema([
  { name: 'Home', url: SITE.url + '/' },
  { name: 'Condo & Townhome Management', url: SITE.url + '/condo-management' },
]);
---
<BaseLayout
  title="Condo Management Built for Shared-Wall Living — CMGT"
  description="Condo management for the Gulf South — condominium associations, townhomes, and high-rises. Shared-building expertise and on-site teams where it counts."
  pageSchema={breadcrumb}
>
  <nav id="site-breadcrumbs" aria-label="Breadcrumb" data-crumbs='[{"t":"Home","href":"/"},{"t":"Condo & Townhome Management"}]'></nav>

  <!-- HERO -->
  <section class="ph-hero">
    <div class="container">
      <div class="ph-hero-top">
        <div>
          <div class="eyebrow">Condo &amp; Townhome Management</div>
          <h1 class="ph-hero-display">Condo management for <em>shared-wall living.</em></h1>
        </div>
        <div>
          <p class="ph-hero-lede">
            Condominiums, townhome associations, and high-rises come with realities a
            single-family HOA never sees — shared structures, building systems, and reserves
            that have to hold up. <strong>We manage the building and the relationships inside it.</strong>
          </p>
          <div class="ph-hero-ctas">
            <a class="btn btn-green" href="/request-a-proposal">Request a proposal <span class="arrow">→</span></a>
            <a class="btn btn-outline-light" href="/how-we-work">How we work</a>
          </div>
        </div>
      </div>
    </div>
    <div class="ph-hero-band">
      <ImageSlot src="/photos/condo-hero-photo.webp" alt="A Gulf South condominium and townhome community — a mid-rise building with balconies and shared grounds under a clear coastal sky" shape="rect" />
    </div>
    <div class="container">
      <div class="ph-hero-stats">
        <div><div class="ph-stat-num" data-count-to="400" data-suffix="+" data-duration="1500">0+</div><div class="ph-stat-label">communities managed</div></div>
        <div><div class="ph-stat-num" data-count-to="96" data-suffix="%" data-duration="1600">0%</div><div class="ph-stat-label">client retention</div></div>
        <div><div class="ph-stat-num" data-count-to="5" data-duration="900">0</div><div class="ph-stat-label">states across the Gulf South</div></div>
        <div><div class="ph-stat-num" data-count-to="2007" data-plain data-duration="1700">0</div><div class="ph-stat-label">independent since</div></div>
      </div>
    </div>
  </section>

  <!-- WHAT'S DIFFERENT -->
  <section class="inc">
    <div class="container">
      <div class="inc-head reveal">
        <div class="eyebrow eyebrow-blue">Why condos aren't just small HOAs</div>
        <h2 class="inc-h">Shared walls change everything.</h2>
        <p class="inc-sub">When owners share a roof, an elevator, and a structural envelope, the stakes are different. <strong>Condo management is its own discipline</strong> — and we staff it that way.</p>
      </div>
      <div class="inc-grid">
        <div class="inc-item reveal"><div class="inc-num">01</div><h3 class="inc-t">Shared-Building Systems</h3><p class="inc-d">Roofs, elevators, structural envelopes, and common mechanicals — managed with vendors who know commercial-grade buildings.</p></div>
        <div class="inc-item reveal"><div class="inc-num">02</div><h3 class="inc-t">Reserves That Hold Up</h3><p class="inc-d">Condo reserves carry real structural risk. We plan, fund, and report them with full transparency — financials to every owner by the 20th.</p></div>
        <div class="inc-item reveal"><div class="inc-num">03</div><h3 class="inc-t">Insurance Complexity</h3><p class="inc-d">Master policies, unit-owner coverage, and claims coordination — navigated with Gulf South storm realities front of mind.</p></div>
        <div class="inc-item reveal"><div class="inc-num">04</div><h3 class="inc-t">Tighter Communities</h3><p class="inc-d">Shared walls mean closer neighbors. Consistent, compassionate enforcement keeps the peace without the friction.</p></div>
        <div class="inc-item reveal"><div class="inc-num">05</div><h3 class="inc-t">On-Site When Needed</h3><p class="inc-d">Front desks, valets, and maintenance staff — we place dedicated people on-site for buildings that require it.</p></div>
        <div class="inc-item reveal"><div class="inc-num">06</div><h3 class="inc-t">Vendor &amp; Project Oversight</h3><p class="inc-d">From envelope repairs to amenity upgrades — competitively bid, scoped, and supervised, with insurance and licensing verified before anyone sets foot on the property.</p></div>
      </div>
    </div>
  </section>

  <!-- TEAM-BASED MODEL (condo) -->
  <section class="team">
    <div class="leaf" aria-hidden="true"><img src="/assets/leaf-mark.svg" alt=""></div>
    <div class="container team-in">
      <div class="reveal">
        <div class="eyebrow" style="color: rgba(255,255,255,.6);">The team-based model</div>
        <h2 class="team-h">One manager you'll know — <em>and a whole department behind them.</em></h2>
        <p class="team-p">In a shared building, you can't afford the industry's default: one overworked manager juggling everything until something slips. <strong>That model fails boards every day</strong> — and if your last company left you feeling burned, it's usually why.</p>
        <p class="team-p">We built CMGT differently. You get one community manager who knows your building by name, backed by full specialist departments. <strong>When one person is out, the work doesn't stop</strong> — and nothing about your roof, your reserves, or your insurance rides on one person having a good day.</p>
        <p class="team-p">And they're our neighbors, too — <strong>local teams who know Gulf South storms, the building codes, and the streets your community sits on.</strong></p>
      </div>
      <div class="team-roles reveal">
        <div class="team-role lead"><h3 class="team-role-t">Your community manager</h3><p class="team-role-d">One name you'll know — the point person who knows your building and your board.</p></div>
        <div class="team-role"><h3 class="team-role-t">Accounting</h3><p class="team-role-d">Reserves, dues, and full financials to every owner by the 20th.</p></div>
        <div class="team-role"><h3 class="team-role-t">Covenants &amp; compliance</h3><p class="team-role-d">Consistent, compassionate enforcement that keeps close neighbors at peace.</p></div>
        <div class="team-role"><h3 class="team-role-t">Vendors &amp; projects</h3><p class="team-role-d">Envelope repairs to amenity upgrades — scoped, bid, and supervised.</p></div>
        <div class="team-role"><h3 class="team-role-t">Communication</h3><p class="team-role-d">Owner updates, meeting support, and answers that don't sit in an inbox.</p></div>
        <div class="team-role"><h3 class="team-role-t">Insurance oversight</h3><p class="team-role-d">Master policy and coverage monitored continuously — written into the agreement.</p></div>
      </div>
    </div>
  </section>

  <!-- WHAT WE HANDLE (condo tabbed deep-dive) -->
  <section class="cov">
    <div class="container">
      <div class="cov-head reveal">
        <div class="eyebrow eyebrow-blue">Full-service, in detail</div>
        <h2 class="cov-h">What "we handle it" actually means.</h2>
        <p class="cov-sub">A shared building has more moving parts than a subdivision — and your board carries the liability for all of them. Here's exactly what you hand off.</p>
      </div>
      <div class="cov-grid reveal">
        <div class="cov-tabs" id="cov-tabs" role="tablist"></div>
        <div class="cov-panel">
          <h3 class="cov-panel-t" id="cov-t">Financial management &amp; reporting</h3>
          <p class="cov-panel-p" id="cov-p">Condo budgets carry bigger numbers and sharper scrutiny — every owner sees where their assessment goes. We keep the books current, transparent, and audit-ready, with the full picture shared every month, not just at tax time.</p>
          <ul class="cov-list" id="cov-list">
            <li>Annual budget preparation</li>
            <li>Operating &amp; reserve fund accounting</li>
            <li>Monthly assessment billing &amp; collection</li>
            <li>Delinquency management</li>
            <li>Monthly P&amp;L and balance sheet by the 20th</li>
            <li>Bank reconciliations</li>
            <li>Audit &amp; CPA coordination</li>
            <li>Owner-facing financial transparency</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- PROPERTY TYPES -->
  <section class="types">
    <div class="container">
      <div class="types-head reveal">
        <div class="eyebrow eyebrow-blue">What we manage</div>
        <h2 class="types-h">One approach, every kind of attached community.</h2>
        <p class="types-sub">From two-story townhome rows to amenity-rich high-rises — the model flexes to the building.</p>
      </div>
      <div class="types-row">
        <div class="type-card reveal">
          <div class="type-tag">Townhome Association Management</div>
          <h3 class="type-t">Townhome associations</h3>
          <p class="type-d">Townhome communities blend HOA and condo realities — shared walls and roofs, but individual entrances and yards. <strong>We handle the maintenance lines that always cause disputes</strong> — who owns the roof, the siding, the fence — with clear policies and consistent enforcement.</p>
        </div>
        <div class="type-card reveal">
          <div class="type-tag">High-Rise Condo Management</div>
          <h3 class="type-t">High-rise &amp; mid-rise condos</h3>
          <p class="type-d">High-rises run like small hospitality operations: elevators, life-safety systems, staffed lobbies, and 24/7 expectations. <strong>We place dedicated on-site teams</strong> and back them with the same specialist bench and financial transparency every CMGT community gets.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- INTERACTIVE: Who's responsible? -->
  <section class="resp">
    <div class="container">
      <div class="resp-head reveal">
        <div class="eyebrow eyebrow-blue">Clear the air</div>
        <h2 class="resp-h">"Wait — who fixes that?"</h2>
        <p class="resp-sub">The single most common source of friction in attached communities is who's responsible for what. Tap an element to see the typical split — then we make it unmistakable in writing.</p>
      </div>
      <div class="resp-grid reveal">
        <div class="resp-chips" id="resp-chips" role="tablist"></div>
        <div class="resp-panel">
          <h3 class="resp-panel-t" id="resp-title"></h3>
          <div class="resp-split">
            <div class="resp-box assoc"><div class="resp-box-h">Usually the association</div><p class="resp-box-d" id="resp-assoc"></p></div>
            <div class="resp-box"><div class="resp-box-h">Usually the owner</div><p class="resp-box-d" id="resp-owner"></p></div>
          </div>
          <p class="resp-note" id="resp-note"></p>
        </div>
      </div>
      <p class="resp-foot reveal">Typical splits shown for illustration — your community's CC&Rs and bylaws are the final word. Clarifying these lines is one of the first things we do.</p>
    </div>
  </section>

  <!-- STORM, INSURANCE & RESERVES -->
  <section class="risk">
    <div class="container">
      <div class="risk-head reveal">
        <div class="eyebrow eyebrow-blue">Built for Gulf South buildings</div>
        <h2 class="risk-h">The risks a shared roof can't ignore.</h2>
        <p class="risk-sub">In an attached community, one failure isn't one owner's problem — it's everyone's. <strong>The three things that strain condo budgets most are storms, insurance gaps, and underfunded reserves.</strong> We manage all three head-on.</p>
      </div>
      <div class="risk-grid">
        <div class="risk-card reveal">
          <div class="risk-k">Hurricane readiness</div>
          <h3 class="risk-t">Plans in place before the season — not during it.</h3>
          <p class="risk-d">Vendor mobilization, owner updates, and recovery coordination when storms hit. <strong>In a shared building, a slow response means water in everyone's units</strong> — so we build the plan before June 1.</p>
        </div>
        <div class="risk-card reveal">
          <div class="risk-k">Insurance monitoring</div>
          <h3 class="risk-t">Master policy, unit coverage, and the gaps between.</h3>
          <p class="risk-d">We monitor your association's coverage continuously — <strong>it's written into our management agreement</strong>, and it's the responsibility most managers quietly leave to the board.</p>
        </div>
        <div class="risk-card reveal">
          <div class="risk-k">Reserves that hold up</div>
          <h3 class="risk-t">When the roof or elevator ages out, the money's there.</h3>
          <p class="risk-d">Big-ticket shared systems fail on their own schedule. We plan and fund reserves in the open — <strong>full financials to every owner by the 20th</strong> — and reassess as the building ages.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ON-SITE -->
  <section class="mdl">
    <div class="container mdl-grid">
      <div class="reveal">
        <div class="eyebrow eyebrow-blue">On-site, when the building calls for it</div>
        <h2 class="mdl-h">A team in the building — <em>and a team behind them.</em></h2>
        <p class="mdl-p">
          Some buildings need someone on-site every day — a high-rise, a large condo community,
          anywhere 500-plus units lean on amenities, life-safety systems, and a staffed lobby.
          When yours does, we place a <strong>dedicated on-site team</strong>: a community manager,
          maintenance, and groundskeeping, right in the building.
        </p>
        <p class="mdl-p">
          <strong>No single point of failure — that's the point.</strong> The on-site team owns the
          daily reality — site inspections, vendor monitoring, amenity management, and in-person
          answers for owners — but they're never on an island. The same specialist departments in
          accounting, covenants, and insurance stand behind them, so nothing about your building
          rides on one person having a good day.
        </p>
        <a class="mdl-link" href="/how-we-work">See how the team-based model works <span class="arrow">→</span></a>
      </div>
      <div class="mdl-media reveal">
        <ImageSlot src="/photos/condo-onsite-photo.webp" alt="An on-site CMGT staff member greeting a resident in the staffed lobby of a Gulf South condominium building" shape="rounded" radius={20} />
      </div>
    </div>
  </section>

  <!-- MARKETS (condo, local presence) -->
  <section class="mkts">
    <div class="container">
      <div class="mkts-head reveal">
        <div class="eyebrow eyebrow-blue">Local, where your building is</div>
        <h2 class="mkts-h">We're not running your building from another time zone.</h2>
        <p class="mkts-sub">CMGT is a Gulf South company with local teams in five states — <strong>neighbors in every one.</strong> The people managing your condo know the coast, the building codes, and what hurricane season actually does here, because they live here too.</p>
      </div>
      <div class="mkts-grid reveal">
        <div class="mkt-card">
          <h3 class="mkt-state">Louisiana</h3>
          <div class="mkt-tag">Where we started</div>
          <p class="mkt-cities">Baton Rouge · New Orleans · North Shore / Hammond · Lafayette · Houma · Lake Charles · Shreveport · Monroe</p>
        </div>
        <div class="mkt-card">
          <h3 class="mkt-state">Mississippi</h3>
          <div class="mkt-tag">Gulf Coast</div>
          <p class="mkt-cities">Biloxi · Gulfport · Bay St. Louis · Ocean Springs · Pass Christian · Diamondhead · Long Beach · D'Iberville</p>
        </div>
        <div class="mkt-card">
          <h3 class="mkt-state">Alabama</h3>
          <div class="mkt-tag">Gulf Coast</div>
          <p class="mkt-cities">Fairhope · Foley · Silverhill · Grand Bay · Semmes</p>
        </div>
        <div class="mkt-card">
          <h3 class="mkt-state">Texas</h3>
          <div class="mkt-tag">Growing market</div>
          <p class="mkt-cities">Orange · East Texas — and growing</p>
        </div>
      </div>
      <div class="mkts-fl reveal">
        <p><strong>A note on Florida:</strong> in the Florida Panhandle we provide full HOA management for non-condo communities — single-family HOAs, master-planned communities, and on-site teams. The one thing we don't take on there is condominium regimes, where the state's regulations aren't a fit for the service standard we insist on. <strong>Condo management is available across our four other states.</strong></p>
      </div>
    </div>
  </section>

  <!-- WHY CMGT (condo) -->
  <section class="vs">
    <div class="leaf" aria-hidden="true"><img src="/assets/leaf-mark.svg" alt=""></div>
    <div class="container vs-in">
      <div class="vs-head reveal">
        <div class="eyebrow" style="color: rgba(255,255,255,.6);">Local vs. national</div>
        <h2 class="vs-h">Big enough to protect your building. <em>Independent enough to mean it.</em></h2>
        <p class="vs-sub">High-rises and luxury condos are where the national brands compete hardest — and most have been bought by private equity or rolled up by out-of-state owners. We haven't — and your building feels the difference.</p>
      </div>
      <div class="vs-toggle reveal" role="tablist">
        <button class="vs-tbtn on" data-v="cmgt" type="button">CMGT</button>
        <button class="vs-tbtn" data-v="chain" type="button">A national firm</button>
      </div>
      <div class="vs-rows reveal" id="vs-rows"></div>
      <p class="vs-foot reveal">Independently owned since 2007 · 100% organic growth · decisions made here in the Gulf South.</p>
    </div>
  </section>

  <!-- FULL-BLEED (condo) -->
  <section class="fbleed">
    <ImageSlot src="/photos/condo-community.jpg" alt="A wide exterior view of a Gulf South high-rise condominium community against a clear coastal sky" shape="rect" />
    <div class="container fbleed-in">
      <div class="eyebrow">Why it matters</div>
      <p class="fbleed-q">We manage communities, <span class="g">not just buildings.</span></p>
      <p class="fbleed-sub">Behind every reserve line and master policy are the people who live under that shared roof. <strong>We never forget the building is full of neighbors.</strong></p>
    </div>
  </section>

  <!-- SCOPE NOTE -->
  <section class="scope">
    <div class="container scope-in">
      <span class="scope-ico" aria-hidden="true"><img src="/assets/leaf-mark.svg" alt=""></span>
      <p class="scope-t">A note on coverage: in Florida we manage non-condo communities — single-family HOAs and master-planned communities — but <strong>not condominium regimes</strong>; the regulatory environment there isn't a fit for the quality of service we insist on. Condo management is available across our four other states.</p>
    </div>
  </section>

  <!-- WHAT IT COSTS (condo) -->
  <section class="cost">
    <div class="container cost-grid">
      <div class="reveal">
        <div class="eyebrow eyebrow-blue">What it costs</div>
        <h2 class="cost-h">What does condo management cost?</h2>
        <p class="cost-p">The honest answer: <strong>it depends on the building</strong> — so we quote custom every time, never a one-size template. A staffed high-rise and a 20-unit townhome row are different operations.</p>
        <p class="cost-p">We compete on <strong>value</strong>, not on being the cheapest bid. Better-funded reserves, monitored insurance, and fewer expensive surprises protect your owners far more than a low monthly fee ever will.</p>
        <p class="cost-note">Want a real number? Request a proposal and we'll scope a quote to your building — no pressure, no obligation.</p>
      </div>
      <div class="cost-factors reveal">
        <div class="cost-frow"><span class="cost-fnum">01</span><div><h3 class="cost-ft">Building type &amp; size</h3><p class="cost-fd">Unit count, shared systems, and amenities — a high-rise carries far more to manage than a townhome community.</p></div></div>
        <div class="cost-frow"><span class="cost-fnum">02</span><div><h3 class="cost-ft">On-site staffing</h3><p class="cost-fd">A desk in the lobby, a valet, or daily maintenance staff — you pay for the on-site presence the building actually needs.</p></div></div>
        <div class="cost-frow"><span class="cost-fnum">03</span><div><h3 class="cost-ft">Insurance &amp; reserve complexity</h3><p class="cost-fd">Master policies and structural reserves take more oversight in attached buildings — and it's where good management pays for itself.</p></div></div>
      </div>
    </div>
  </section>

  <!-- SWITCHING CALLOUT (condo) -->
  <section class="swcall">
    <div class="container">
      <div class="swcall-in reveal">
        <div class="swcall-txt">
          <div class="swcall-k">Already have a manager?</div>
          <h2 class="swcall-h">Burned by your last company? Switching is easier than you think.</h2>
          <p class="swcall-p">In a shared building, slow responses and surprise assessments hurt everyone at once. We handle the whole transition for you — records, vendors, finances, and owners — on a clear Day 1 to Day 90 plan, so the change feels like relief.</p>
        </div>
        <a class="btn btn-blue swcall-cta" href="/switching-hoa-management-companies">See how switching works <span class="arrow">→</span></a>
      </div>
    </div>
  </section>

  <!-- 3 C's -->
  <section class="ccc">
    <div class="container ccc-in reveal">
      <div class="eyebrow eyebrow-blue">Why condo boards choose CMGT</div>
      <p class="ccc-q">"We <span class="g">commit</span> because communities matter. We <span class="g">communicate</span> because people matter. We <span class="g">care</span> because that's what matters most."</p>
    </div>
  </section>

  <!-- PEER -->
  <section class="peers">
    <div class="container">
      <div class="peers-head reveal">
        <div class="eyebrow eyebrow-blue">Managing a different kind of community?</div>
        <h2 class="peers-h">We have a dedicated practice for that, too.</h2>
        <p class="peers-sub">Condo management is one of several full practices at CMGT — each built for a distinct kind of community.</p>
      </div>
      <div class="peers-grid">
        <a class="peer-card reveal" href="/hoa-management-services">
          <h3 class="peer-t">HOA Management Services</h3>
          <p class="peer-d">Full-service management for single-family, townhome, and master-planned associations.</p>
          <span class="peer-cta">Explore HOA management <span class="arrow">→</span></span>
        </a>
        <a class="peer-card reveal" href="/hoa-financial-management">
          <h3 class="peer-t">HOA Financial Management</h3>
          <p class="peer-d">Right-sized financial and administrative support for smaller communities that don't need full-service oversight.</p>
          <span class="peer-cta">Explore financial-only <span class="arrow">→</span></span>
        </a>
        <a class="peer-card reveal" href="/developer-hoa-management">
          <h3 class="peer-t">Developer HOA Management</h3>
          <p class="peer-d">Declarant control through homeowner turnover — structured from groundbreaking onward.</p>
          <span class="peer-cta">Explore developer services <span class="arrow">→</span></span>
        </a>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq">
    <div class="container faq-grid">
      <div class="reveal">
        <div class="eyebrow eyebrow-blue">Common questions</div>
        <h2 class="faq-h">Before you ask.</h2>
      </div>
      <div class="faq-list reveal">
        <div class="faq-item">
          <button class="faq-q" type="button">How is condo management different from HOA management?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">Condos share structural elements — roofs, elevators, building envelopes — so reserves, insurance, and maintenance carry more risk and complexity. <strong>We staff condo management as its own discipline</strong>, with vendors and on-site options built for shared buildings.</div></div>
        </div>
        <div class="faq-item">
          <button class="faq-q" type="button">Do you provide on-site staff for high-rises?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">Yes. For buildings that need a daily presence, we place dedicated on-site managers and staff — <strong>backed by the same specialist team</strong> every CMGT community relies on.</div></div>
        </div>
        <div class="faq-item">
          <button class="faq-q" type="button">Do you manage townhome associations?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">Absolutely. Townhomes sit between HOA and condo realities, and the maintenance-responsibility lines are where disputes start. <strong>We set clear policies and enforce them consistently</strong> so neighbors stay neighbors.</div></div>
        </div>
        <div class="faq-item">
          <button class="faq-q" type="button">Do you manage condos in Florida?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">No — condominium regimes are the one thing we don't take on in Florida; the regulatory environment isn't a fit for the service standard we hold ourselves to. We do provide full HOA management for non-condo Florida communities, though, and <strong>condo management across our four other states.</strong></div></div>
        </div>
        <div class="faq-item">
          <button class="faq-q" type="button">How do you handle reserves and big-ticket repairs?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">Shared systems — roofs, elevators, building envelopes — fail on their own timeline. We plan and fund reserves with full transparency, <strong>report financials to every owner by the 20th</strong>, and reassess as the building ages so a major repair doesn't become a surprise assessment.</div></div>
        </div>
        <div class="faq-item">
          <button class="faq-q" type="button">Who's responsible for insurance — the association or the owner?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">Both — and the line between a master policy and unit-owner coverage is exactly where claims go wrong. <strong>We monitor your association's coverage continuously — it's written into our agreement</strong> — and coordinate claims so nothing falls through the gap after a storm.</div></div>
        </div>
        <div class="faq-item">
          <button class="faq-q" type="button">What about hurricane season?<span class="faq-sign" aria-hidden="true"></span></button>
          <div class="faq-a"><div class="faq-a-in">We build the storm plan before June 1 — not during the warnings. That means emergency vendors lined up before they're scarce, your master insurance reviewed so coverage gaps don't surface mid-claim, and an evacuation and communication plan owners actually have in hand. When weather hits, we mobilize trusted vendors, document damage for the claim, and manage the recovery — from water mitigation to a full rebuild — because in a shared building, a slow response means water in everyone's units. <strong>Gulf South weather is the reality we manage around, not an afterthought.</strong></div></div>
        </div>
      </div>
    </div>
  </section>

  <!-- BOARD EDUCATION (condo) -->
  <section class="edu">
    <div class="container">
      <div class="edu-head reveal">
        <div class="eyebrow eyebrow-blue">Board education</div>
        <h2 class="edu-h">Running a condo or townhome board? Start here.</h2>
        <p class="edu-sub">Plain-English guides from our Board Education Hub — written for volunteers, not insiders. No sales pitch, just the stuff that helps.</p>
      </div>
      <div class="edu-grid">
        <a class="edu-card reveal" href="/resources/hoa-reserve-study">
          <span class="edu-cat">Finances</span>
          <h3 class="edu-t">What is a reserve study?</h3>
          <p class="edu-d">When the roof, elevator, or envelope wears out, will the money be there? How reserve studies work — and how often to refresh them.</p>
          <span class="edu-go">Read the guide <span class="arrow">→</span></span>
        </a>
        <a class="edu-card reveal" href="/resources/hurricane-preparedness-for-hoas">
          <span class="edu-cat">Insurance &amp; Risk</span>
          <h3 class="edu-t">Hurricane prep for Gulf South boards</h3>
          <p class="edu-d">Insurance, reserves, vendor contracts, and the communication plan to lock down before the season hits.</p>
          <span class="edu-go">Read the guide <span class="arrow">→</span></span>
        </a>
        <a class="edu-card reveal" href="/hoa-financial-management">
          <span class="edu-cat">Finances</span>
          <h3 class="edu-t">Keeping delinquency low, fairly</h3>
          <p class="edu-d">A consistent, compassionate collections process protects the budget — and the neighbors who pay on time.</p>
          <span class="edu-go">See how it works <span class="arrow">→</span></span>
        </a>
        <a class="edu-card reveal" href="/switching-hoa-management-companies">
          <span class="edu-cat">Solutions</span>
          <h3 class="edu-t">Switching without the disruption</h3>
          <p class="edu-d">What a clean Day 1 to Day 90 transition looks like — and how we keep your building from ever feeling the gap.</p>
          <span class="edu-go">See the plan <span class="arrow">→</span></span>
        </a>
        <a class="edu-card reveal" href="/self-managed-hoa">
          <span class="edu-cat">Solutions</span>
          <h3 class="edu-t">Self-managed vs. professional</h3>
          <p class="edu-d">An honest comparison for boards weighing whether it's time to bring in a team — and a middle path.</p>
          <span class="edu-go">See the comparison <span class="arrow">→</span></span>
        </a>
        <a class="edu-card reveal" href="/resources/hoa-budget-template">
          <span class="edu-cat">Finances</span>
          <h3 class="edu-t">Build a budget that holds</h3>
          <p class="edu-d">How to fund reserves and avoid surprise assessments — and set up a budgeting process your board can run every year.</p>
          <span class="edu-go">Read the guide <span class="arrow">→</span></span>
        </a>
      </div>
      <a class="edu-all reveal" href="/resources">Explore the full Board Education Hub <span class="arrow">→</span></a>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta">
    <div class="leaf" aria-hidden="true"><img src="/assets/leaf-mark.svg" alt=""></div>
    <div class="container cta-in">
      <div class="reveal">
        <h2 class="cta-h">Let's talk about your community.</h2>
        <p class="cta-p">Tell us about your condo or townhome community and a real person will reach out within two business days. No pitch deck, no pressure.</p>
      </div>
      <div class="cta-actions reveal">
        <a class="btn btn-blue" href="/request-a-proposal">Request a proposal <span class="arrow">→</span></a>
        <a class="cta-phone" href="tel:2255032648">Or call (225) 503-2648 · Mon–Fri, 8:30a–4:30p CT</a>
      </div>
    </div>
  </section>

  <script>
    (function () {
      var COV = [
        { tab: 'Financials', t: 'Financial management & reporting', p: "Condo budgets carry bigger numbers and sharper scrutiny — every owner sees where their assessment goes. We keep the books current, transparent, and audit-ready, with the full picture shared every month, not just at tax time.", items: ['Annual budget preparation', 'Operating & reserve fund accounting', 'Monthly assessment billing & collection', 'Delinquency management', 'Monthly P&L and balance sheet by the 20th', 'Bank reconciliations', 'Audit & CPA coordination', 'Owner-facing financial transparency'] },
        { tab: 'Insurance & reserves', t: 'Insurance & reserve oversight', p: "In an attached building, the line between the master policy and a unit owner's coverage is exactly where claims go wrong. We monitor your association's coverage and fund reserves for the systems everyone shares — so the money is there before the roof or elevator needs it.", items: ['Master insurance policy review', 'Coverage gap & deductible analysis', 'Claims coordination', 'Reserve study scheduling', 'Reserve funding strategy', 'Loss assessment guidance', 'Certificate of insurance tracking', 'Annual renewal management'] },
        { tab: 'Building maintenance', t: 'Building & common-element maintenance', p: "The roof, the elevator, the hallways, the envelope — in a condo these belong to everyone, and they fail on their own timeline. We keep the shared systems inspected, serviced, and ahead of trouble, with vetted vendors and accountable quality control.", items: ['Roof & building-envelope upkeep', 'Elevator service contracts', 'Common-area & hallway maintenance', 'Amenity & pool management', 'Landscaping & exterior care', 'Preventive maintenance scheduling', 'Vendor monitoring & QC', 'Routine site inspections'] },
        { tab: 'Owner communication', t: 'Owner & resident communication', p: "A building full of owners — plus renters and absentee owners — generates more questions, faster. We answer promptly in plain language, and give every owner a 24/7 portal for payments, requests, and documents.", items: ['Prompt owner responses', '24/7 owner portal', 'Online assessment payments', 'Building-wide notifications', 'Newsletters & announcements', 'Document library access', 'Renter & absentee-owner coordination', 'Move-in / move-out support'] },
        { tab: 'Major projects', t: 'Capital & major projects', p: "A re-roof, a façade repair, an elevator modernization — the big projects are where condo boards feel most exposed. We scope the work, coordinate engineers and contractors, plan the funding, and keep owners informed from first bid to closeout.", items: ['Competitive bidding & scoping', 'Engineer & contractor coordination', 'Special-assessment planning', 'Construction oversight', 'Reserve-funded project management', 'Permit & compliance tracking', 'Owner updates through the work', 'Warranty & closeout'] },
        { tab: 'Emergency response', t: 'Emergency & storm response', p: "This is the Gulf South, and a shared building can't afford a slow response — one delay means water in everyone's units. We plan before the season, mobilize trusted vendors when weather hits, coordinate evacuation and re-entry, and manage the recovery.", items: ['Pre-season preparedness planning', 'Emergency vendor mobilization', 'Water-intrusion rapid response', 'Evacuation & re-entry coordination', 'Insurance claim coordination', 'Real-time owner updates', 'Post-storm damage assessment', 'Recovery & rebuild management'] }
      ];
      var tabs = document.getElementById('cov-tabs');
      var t = document.getElementById('cov-t'), p = document.getElementById('cov-p'), list = document.getElementById('cov-list');
      if (tabs) {
        tabs.innerHTML = COV.map(function (c, i) { return '<button class="cov-tab" role="tab" data-i="' + i + '"><span class="n">0' + (i + 1) + '</span>' + c.tab + '</button>'; }).join('');
        function sel(i) {
          var c = COV[i];
          t.textContent = c.t; p.textContent = c.p;
          list.innerHTML = c.items.map(function (x) { return '<li>' + x + '</li>'; }).join('');
          [].forEach.call(tabs.children, function (b, bi) { b.classList.toggle('on', bi === i); });
        }
        [].forEach.call(tabs.children, function (b) { b.addEventListener('click', function () { sel(+b.dataset.i); }); });
        tabs.children[0].classList.add('on');
      }
    })();
  </script>

  <script>
    (function () {
      var items = [
        { k: 'Roof', assoc: 'Structure, membrane, and shingles on shared roofs.', owner: 'Nothing on shared roofs; interior leaks from owner alterations.', note: 'On most condos the roof is fully association-maintained — it protects everyone under it.' },
        { k: 'Exterior walls & siding', assoc: 'Siding, paint, and the structural envelope.', owner: 'Anything attached by the owner (lights, mounts, satellite dishes).', note: 'Townhomes vary most here — we spell out the line so neighbors don’t guess.' },
        { k: 'Windows & doors', assoc: 'Frames in the common structure (varies widely).', owner: 'Glass, screens, locks, and interior treatments.', note: 'This one is governing-doc dependent — a frequent source of disputes we head off early.' },
        { k: 'Decks, patios & balconies', assoc: 'Structural elements of balconies on shared buildings.', owner: 'Surface finishes, railings upkeep, and personal use items.', note: 'Limited common elements often sit here — owner-used, association-structural.' },
        { k: 'Interior & fixtures', assoc: 'Nothing inside the unit walls.', owner: 'Everything from the paint in: fixtures, appliances, flooring.', note: '“Studs in” is the simple rule of thumb — the owner’s domain.' },
        { k: 'Landscaping & common areas', assoc: 'Shared grounds, amenities, lighting, and irrigation.', owner: 'Private patios or fenced yards designated to the unit.', note: 'Shared greenery is the association’s — it’s a big part of what dues fund.' }
      ];
      var chips = document.getElementById('resp-chips');
      var title = document.getElementById('resp-title'), a = document.getElementById('resp-assoc'), o = document.getElementById('resp-owner'), n = document.getElementById('resp-note');
      if (!chips) return;
      function select(i) {
        [].forEach.call(chips.children, function (c, ci) { c.classList.toggle('on', ci === i); });
        title.textContent = items[i].k;
        a.textContent = items[i].assoc; o.textContent = items[i].owner; n.innerHTML = items[i].note;
      }
      items.forEach(function (it, i) {
        var b = document.createElement('button');
        b.type = 'button'; b.className = 'resp-chip';
        b.innerHTML = '<span>' + it.k + '</span><span class="ar">→</span>';
        b.addEventListener('click', function () { select(i); });
        chips.appendChild(b);
      });
      select(0);
    })();
  </script>

  <script>
    (function () {
      var DATA = {
        cmgt: [
          ['Ownership', 'Independent and founder-led since 2007 — <strong>answers to your board, not to investors</strong>.'],
          ['Decisions', 'Made here in the Gulf South, by people who <strong>know hurricane season and your building codes</strong>.'],
          ['On-site', 'A manager we can place in your building when it needs one — <strong>backed by a full specialist bench</strong>.'],
          ['Insurance & reserves', 'Coverage monitored continuously and reserves planned in the open — <strong>the responsibilities most managers avoid</strong>.'],
          ['Financials', '<strong>Shared with every owner by the 20th</strong> — transparency written into the agreement.']
        ],
        chain: [
          ['Ownership', 'Often private-equity owned, with cost targets set far from your building.'],
          ['Decisions', 'Policies set at a distant HQ with little feel for Gulf South storms or local codes.'],
          ['On-site', 'High portfolio loads and turnover — you may not know who your manager is this quarter.'],
          ['Insurance & reserves', 'Insurance monitoring is often disclaimed; reserve transparency varies widely.'],
          ['Financials', 'Reporting varies; full transparency to every owner is rarely the default.']
        ]
      };
      var rows = document.getElementById('vs-rows');
      var btns = document.querySelectorAll('.vs-tbtn');
      if (!rows) return;
      function render(v) {
        rows.innerHTML = DATA[v].map(function (r) {
          return '<div class="vs-row"><div class="vs-aspect">' + r[0] + '</div><div class="vs-val">' + r[1] + '</div></div>';
        }).join('');
        btns.forEach(function (b) { b.classList.toggle('on', b.dataset.v === v); });
      }
      btns.forEach(function (b) { b.addEventListener('click', function () { render(b.dataset.v); }); });
      render('cmgt');
    })();
  </script>

  <script>
    (function () {
      document.querySelectorAll('.faq-item').forEach(function (item) {
        var btn = item.querySelector('.faq-q');
        var ans = item.querySelector('.faq-a');
        btn.addEventListener('click', function () {
          var open = item.classList.toggle('open');
          ans.style.maxHeight = open ? (ans.scrollHeight + 'px') : '0';
        });
      });
    })();
  </script>
</BaseLayout>

<style>
  body { background: #fff; }
  .eyebrow-blue { color: var(--freedom-blue); opacity: .6; }

  /* HERO (shared pillar template) */
  .ph-hero { background: var(--freedom-blue); color: #fff; padding: 96px 0 0; position: relative; overflow: hidden; isolation: isolate; }
  .ph-hero::after { content: ""; position: absolute; inset: 0; background: radial-gradient(circle at 16% 18%, rgba(141,189,255,.12) 0%, transparent 55%); z-index: 0; pointer-events: none; }
  .ph-hero-top { position: relative; z-index: 2; display: grid; grid-template-columns: 1.15fr .85fr; gap: 64px; align-items: end; padding-bottom: 64px; }
  @media (max-width: 980px) { .ph-hero-top { grid-template-columns: 1fr; gap: 32px; } }
  .ph-hero .eyebrow { color: rgba(255,255,255,.65); }
  .ph-hero-display { font-family: var(--font-display); font-weight: 900; font-size: clamp(44px, 5.6vw, 84px); line-height: .99; letter-spacing: -.03em; color: #fff; margin: 22px 0 0; text-wrap: balance; }
  .ph-hero-display em { font-style: italic; font-weight: 300; color: var(--landscaping-green); }
  .ph-hero-lede { font-size: 17.5px; line-height: 1.7; color: rgba(255,255,255,.82); margin: 0 0 30px; }
  .ph-hero-lede strong { color: #fff; font-weight: 600; }
  .ph-hero-ctas { display: flex; gap: 14px; flex-wrap: wrap; }
  .ph-hero-band { position: relative; z-index: 2; }
  .ph-hero-band :global(.image-slot) { display: block; width: 100%; height: clamp(280px, 32vw, 420px); }
  .ph-hero-stats { position: relative; z-index: 2; display: grid; grid-template-columns: repeat(4, 1fr); gap: 28px; padding: 30px 0; border-top: 1px solid rgba(255,255,255,.18); }
  @media (max-width: 620px) { .ph-hero-stats { grid-template-columns: 1fr 1fr; gap: 22px; } }
  .ph-stat-num { font-family: var(--font-display); font-weight: 700; font-size: 40px; line-height: 1; letter-spacing: -.02em; color: #fff; }
  .ph-stat-label { font-size: 12.5px; color: rgba(255,255,255,.6); margin-top: 7px; }

  /* WHAT'S DIFFERENT */
  .inc { background: #fff; padding: 112px 0; }
  .inc-head { max-width: 760px; margin: 0 0 56px; }
  .inc-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(30px, 3.8vw, 46px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .inc-sub { font-size: 16.5px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .inc-sub strong { color: var(--freedom-blue); font-weight: 600; }
  .inc-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px 44px; }
  @media (max-width: 880px) { .inc-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 560px) { .inc-grid { grid-template-columns: 1fr; } }
  .inc-item { border-top: 3px solid var(--landscaping-green); padding-top: 22px; }
  .inc-num { font-family: var(--font-body); font-weight: 700; font-size: 12px; letter-spacing: .16em; color: var(--ink-400); }
  .inc-t { font-family: var(--font-display); font-weight: 700; font-size: 22px; line-height: 1.15; color: var(--freedom-blue); margin: 10px 0 10px; }
  .inc-d { font-size: 14.5px; line-height: 1.65; color: var(--ink-500); margin: 0; }

  /* PROPERTY TYPES (townhome + high-rise H2s) */
  .types { background: var(--nb-lightest); padding: 110px 0; }
  .types-head { max-width: 720px; margin: 0 0 50px; }
  .types-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.4vw, 42px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .types-sub { font-size: 16px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .types-row { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
  @media (max-width: 820px) { .types-row { grid-template-columns: 1fr; } }
  .type-card { background: #fff; border-radius: 16px; padding: 38px 36px; box-shadow: var(--shadow-2); }
  .type-tag { font-size: 11px; font-weight: 700; letter-spacing: .16em; text-transform: uppercase; color: var(--landscaping-green); }
  .type-t { font-family: var(--font-display); font-weight: 700; font-size: 26px; line-height: 1.12; color: var(--freedom-blue); margin: 12px 0 12px; }
  .type-d { font-size: 15px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .type-d strong { color: var(--freedom-blue); font-weight: 600; }

  /* ON-SITE band */
  .mdl { background: #fff; padding: 110px 0; }
  .mdl-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; }
  @media (max-width: 920px) { .mdl-grid { grid-template-columns: 1fr; gap: 36px; } }
  .mdl-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.4vw, 42px); line-height: 1.14; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 18px; text-wrap: balance; }
  .mdl-h em { font-style: italic; font-weight: 500; }
  .mdl-p { font-size: 16px; line-height: 1.75; color: var(--ink-500); margin: 0 0 16px; max-width: 540px; }
  .mdl-p strong { color: var(--freedom-blue); font-weight: 600; }
  .mdl-link { display: inline-flex; align-items: center; gap: 8px; font-weight: 600; font-size: 15px; color: var(--freedom-blue); text-decoration: none; margin-top: 6px; }
  .mdl-link .arrow { color: var(--landscaping-green); transition: transform var(--dur-base) var(--ease-out); }
  .mdl-link:hover .arrow { transform: translateX(4px); }
  .mdl-media :global(.image-slot) { width: 100%; height: 440px; }

  /* SCOPE NOTE (no FL condos) */
  .scope { background: var(--freedom-blue); color: #fff; padding: 28px 0; }
  .scope-in { display: flex; align-items: center; gap: 16px; justify-content: center; text-align: center; flex-wrap: wrap; }
  .scope-ico { width: 26px; flex: none; }
  .scope-ico img { width: 100%; filter: brightness(0) invert(1); opacity: .8; }
  .scope-t { font-size: 15px; line-height: 1.5; color: rgba(255,255,255,.85); max-width: 760px; }
  .scope-t strong { color: #fff; font-weight: 600; }

  /* 3 C's */
  .ccc { background: var(--nb-lightest); padding: 100px 0; }
  .ccc-in { max-width: 940px; }
  .ccc-q { font-family: var(--font-display); font-weight: 300; font-style: italic; font-size: clamp(26px, 3.4vw, 40px); line-height: 1.3; letter-spacing: -.01em; color: var(--freedom-blue); margin: 16px 0 0; text-wrap: balance; }
  .ccc-q .g { color: var(--landscaping-green); font-style: normal; font-weight: 700; }

  /* PEER (lateral link to HOA head pillar) */
  .peers { background: #fff; padding: 110px 0; }
  .peers-head { max-width: 680px; margin: 0 0 44px; }
  .peers-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.4vw, 42px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .peers-sub { font-size: 16px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .peers-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
  @media (max-width: 880px) { .peers-grid { grid-template-columns: 1fr; } }
  .peer-card { display: block; border: 1.5px solid var(--hairline-strong); border-radius: 16px; padding: 30px 28px; text-decoration: none; transition: border-color var(--dur-base) var(--ease-out), transform var(--dur-base) var(--ease-out); }
  .peer-card:hover { border-color: var(--landscaping-green); transform: translateY(-3px); box-shadow: var(--shadow-2); }
  .peer-t { font-family: var(--font-display); font-weight: 700; font-size: 21px; color: var(--freedom-blue); margin: 0 0 8px; }
  .peer-d { font-size: 14px; line-height: 1.6; color: var(--ink-500); margin: 0 0 16px; }
  .peer-cta { display: inline-flex; align-items: center; gap: 7px; font-weight: 600; font-size: 14px; color: var(--freedom-blue); }
  .peer-cta .arrow { color: var(--landscaping-green); transition: transform var(--dur-base) var(--ease-out); }
  .peer-card:hover .peer-cta .arrow { transform: translateX(4px); }

  /* FAQ */
  .faq { background: var(--nb-lightest); padding: 110px 0; }
  .faq-grid { display: grid; grid-template-columns: .8fr 1.2fr; gap: 56px; align-items: start; }
  @media (max-width: 860px) { .faq-grid { grid-template-columns: 1fr; gap: 24px; } }
  .faq-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.4vw, 42px); line-height: 1.1; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 0; text-wrap: balance; }
  .faq-list { border-top: 1.5px solid var(--hairline-strong); }
  .faq-item { border-bottom: 1.5px solid var(--hairline-strong); }
  .faq-q { width: 100%; display: flex; align-items: center; justify-content: space-between; gap: 20px; background: transparent; border: 0; cursor: pointer; text-align: left; padding: 24px 2px; font-family: var(--font-display); font-weight: 700; font-size: 19px; color: var(--freedom-blue); }
  .faq-sign { flex: none; width: 26px; height: 26px; position: relative; }
  .faq-sign::before, .faq-sign::after { content: ""; position: absolute; background: var(--landscaping-green); border-radius: 2px; transition: transform var(--dur-base) var(--ease-out); }
  .faq-sign::before { left: 0; right: 0; top: 12px; height: 2.5px; }
  .faq-sign::after { top: 0; bottom: 0; left: 12px; width: 2.5px; }
  .faq-item.open .faq-sign::after { transform: scaleY(0); }
  .faq-a { max-height: 0; overflow: hidden; transition: max-height var(--dur-base) var(--ease-out); }
  .faq-a-in { font-size: 15.5px; line-height: 1.7; color: var(--ink-500); padding: 0 2px 24px; max-width: 640px; }
  .faq-a-in strong { color: var(--freedom-blue); font-weight: 600; }

  /* CTA */
  .cta { background: var(--landscaping-green); color: var(--freedom-blue); padding: 100px 0; position: relative; overflow: hidden; }
  .cta .leaf { position: absolute; left: -110px; bottom: -150px; width: 460px; height: 460px; opacity: .14; transform: rotate(14deg); pointer-events: none; }
  .cta .leaf img { filter: brightness(0) saturate(100%) invert(11%) sepia(64%) saturate(4147%) hue-rotate(238deg) brightness(85%) contrast(96%); }
  .cta-in { position: relative; display: flex; align-items: center; justify-content: space-between; gap: 48px; flex-wrap: wrap; }
  .cta-h { font-family: var(--font-display); font-weight: 900; font-size: clamp(34px, 4.6vw, 58px); line-height: 1.02; letter-spacing: -.02em; margin: 0 0 12px; text-wrap: balance; }
  .cta-p { font-size: 17px; line-height: 1.6; max-width: 500px; margin: 0; color: rgba(30,30,119,.82); }
  .cta-actions { display: flex; flex-direction: column; align-items: flex-start; gap: 12px; }
  .cta-phone { font-size: 14px; font-weight: 600; color: rgba(30,30,119,.75); text-decoration: none; }

  /* TEAM-BASED MODEL (condo) */
  .team { background: var(--freedom-blue); color: #fff; padding: 112px 0; position: relative; overflow: hidden; }
  .team .leaf { position: absolute; right: -130px; top: -120px; width: 440px; height: 440px; opacity: .07; transform: rotate(18deg); pointer-events: none; }
  .team .leaf img { width: 100%; filter: brightness(0) invert(1); }
  .team-in { position: relative; z-index: 1; display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: start; }
  @media (max-width: 920px) { .team-in { grid-template-columns: 1fr; gap: 40px; } }
  .team-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: #fff; margin: 16px 0 20px; text-wrap: balance; }
  .team-h em { font-style: italic; font-weight: 300; color: var(--landscaping-green); }
  .team-p { font-size: 16.5px; line-height: 1.75; color: rgba(255,255,255,.82); margin: 0 0 18px; max-width: 520px; }
  .team-p strong { color: #fff; font-weight: 600; }
  .team-roles { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  @media (max-width: 520px) { .team-roles { grid-template-columns: 1fr; } }
  .team-role { background: rgba(255,255,255,.06); border: 1px solid rgba(255,255,255,.14); border-radius: 14px; padding: 22px 22px; }
  .team-role-t { font-family: var(--font-display); font-weight: 700; font-size: 16.5px; color: #fff; margin: 0 0 7px; }
  .team-role-d { font-size: 13.5px; line-height: 1.6; color: rgba(255,255,255,.72); margin: 0; }
  .team-role.lead { background: var(--landscaping-green); border-color: var(--landscaping-green); }
  .team-role.lead .team-role-t { color: var(--freedom-blue); }
  .team-role.lead .team-role-d { color: rgba(30,30,119,.82); }

  /* WHAT WE HANDLE (cov) */
  .cov { background: #fff; padding: 112px 0; }
  .cov-head { max-width: 760px; margin: 0 0 48px; }
  .cov-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .cov-sub { font-size: 16px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .cov-grid { display: grid; grid-template-columns: 300px 1fr; gap: 28px; align-items: start; }
  @media (max-width: 860px) { .cov-grid { grid-template-columns: 1fr; } }
  .cov-tabs { display: flex; flex-direction: column; gap: 4px; }
  @media (max-width: 860px) { .cov-tabs { flex-direction: row; flex-wrap: wrap; gap: 8px; } }
  .cov-tabs :global(.cov-tab) { display: flex; align-items: center; gap: 14px; width: 100%; text-align: left; cursor: pointer; background: transparent; border: 0; border-left: 3px solid transparent; border-radius: 0 8px 8px 0; padding: 16px 16px; font-family: var(--font-body); font-weight: 600; font-size: 15.5px; color: var(--ink-500); transition: all var(--dur-base) var(--ease-out); }
  @media (max-width: 860px) { .cov-tabs :global(.cov-tab) { width: auto; border-left: 0; border-bottom: 3px solid transparent; border-radius: 8px 8px 0 0; padding: 12px 14px; } }
  .cov-tabs :global(.cov-tab:hover) { color: var(--freedom-blue); background: var(--nb-lightest); }
  .cov-tabs :global(.cov-tab.on) { color: var(--freedom-blue); background: var(--nb-lightest); border-left-color: var(--landscaping-green); }
  @media (max-width: 860px) { .cov-tabs :global(.cov-tab.on) { border-left-color: transparent; border-bottom-color: var(--landscaping-green); } }
  .cov-tabs :global(.cov-tab .n) { font-family: var(--font-display); font-weight: 300; font-style: italic; font-size: 20px; color: var(--landscaping-green); flex: none; }
  .cov-panel { background: var(--nb-lightest); border-radius: 18px; padding: 40px 42px; min-height: 300px; }
  @media (max-width: 560px) { .cov-panel { padding: 30px 26px; } }
  .cov-panel-t { font-family: var(--font-display); font-weight: 700; font-size: 26px; line-height: 1.15; color: var(--freedom-blue); margin: 0 0 14px; }
  .cov-panel-p { font-size: 16px; line-height: 1.75; color: var(--ink-700); margin: 0 0 22px; max-width: 640px; }
  .cov-panel-p strong { color: var(--freedom-blue); font-weight: 600; }
  .cov-list { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: 1fr 1fr; gap: 12px 24px; }
  @media (max-width: 560px) { .cov-list { grid-template-columns: 1fr; } }
  .cov-list li { font-size: 14.5px; line-height: 1.5; color: var(--ink-700); padding-left: 24px; position: relative; }
  .cov-list li::before { content: ""; position: absolute; left: 0; top: 7px; width: 8px; height: 8px; border-radius: 50%; background: var(--landscaping-green); }

  /* INTERACTIVE: Who's responsible? */
  .resp { background: #fff; padding: 112px 0; }
  .resp-head { max-width: 720px; margin: 0 0 44px; }
  .resp-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .resp-sub { font-size: 16px; line-height: 1.7; color: var(--ink-500); margin: 0; max-width: 640px; }
  .resp-grid { display: grid; grid-template-columns: 320px 1fr; gap: 28px; align-items: start; }
  @media (max-width: 820px) { .resp-grid { grid-template-columns: 1fr; } }
  .resp-chips { display: flex; flex-direction: column; gap: 8px; }
  .resp-chips :global(.resp-chip) { display: flex; align-items: center; justify-content: space-between; gap: 12px; width: 100%; text-align: left; cursor: pointer; font-family: var(--font-body); font-weight: 600; font-size: 16px; color: var(--freedom-blue); background: #fff; border: 1.5px solid var(--hairline-strong); border-radius: 12px; padding: 16px 18px; transition: border-color var(--dur-base) var(--ease-out), background var(--dur-base) var(--ease-out); }
  .resp-chips :global(.resp-chip:hover) { border-color: var(--landscaping-green); }
  .resp-chips :global(.resp-chip.on) { background: var(--freedom-blue); border-color: var(--freedom-blue); color: #fff; }
  .resp-chips :global(.resp-chip .ar) { opacity: 0; transition: opacity var(--dur-base) var(--ease-out); }
  .resp-chips :global(.resp-chip.on .ar) { opacity: 1; color: var(--landscaping-green); }
  .resp-panel { background: var(--nb-lightest); border-radius: 18px; padding: 38px 38px; min-height: 280px; }
  .resp-panel-t { font-family: var(--font-display); font-weight: 700; font-size: 24px; color: var(--freedom-blue); margin: 0 0 20px; }
  .resp-split { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; margin-bottom: 22px; }
  @media (max-width: 520px) { .resp-split { grid-template-columns: 1fr; } }
  .resp-box { background: #fff; border-radius: 12px; padding: 20px 20px; border-top: 3px solid var(--hairline-strong); }
  .resp-box.assoc { border-top-color: var(--landscaping-green); }
  .resp-box-h { font-size: 11px; font-weight: 700; letter-spacing: .14em; text-transform: uppercase; color: var(--ink-400); margin-bottom: 8px; }
  .resp-box.assoc .resp-box-h { color: var(--landscaping-green); }
  .resp-box-d { font-size: 14.5px; line-height: 1.6; color: var(--ink-700); margin: 0; }
  .resp-note { font-size: 14px; line-height: 1.6; color: var(--ink-500); margin: 0; padding-left: 16px; border-left: 3px solid var(--landscaping-green); }
  .resp-note strong { color: var(--freedom-blue); font-weight: 600; }
  .resp-foot { font-size: 12.5px; color: var(--ink-400); margin: 22px 0 0; }

  /* STORM, INSURANCE & RESERVES */
  .risk { background: var(--nb-lightest); padding: 112px 0; }
  .risk-head { max-width: 760px; margin: 0 0 50px; }
  .risk-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .risk-sub { font-size: 16.5px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .risk-sub strong { color: var(--freedom-blue); font-weight: 600; }
  .risk-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
  @media (max-width: 880px) { .risk-grid { grid-template-columns: 1fr; } }
  .risk-card { background: #fff; border-radius: 16px; padding: 34px 32px; box-shadow: var(--shadow-2); border-top: 3px solid var(--landscaping-green); }
  .risk-k { font-size: 11px; font-weight: 700; letter-spacing: .16em; text-transform: uppercase; color: var(--landscaping-green); }
  .risk-t { font-family: var(--font-display); font-weight: 700; font-size: 21px; line-height: 1.16; color: var(--freedom-blue); margin: 12px 0 12px; }
  .risk-d { font-size: 14.5px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .risk-d strong { color: var(--freedom-blue); font-weight: 600; }

  /* MARKETS (condo, local presence) */
  .mkts { background: var(--nb-lightest); padding: 112px 0; }
  .mkts-head { max-width: 720px; margin: 0 0 48px; }
  .mkts-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .mkts-sub { font-size: 16.5px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .mkts-sub strong { color: var(--freedom-blue); font-weight: 600; }
  .mkts-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }
  @media (max-width: 920px) { .mkts-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 540px) { .mkts-grid { grid-template-columns: 1fr; } }
  .mkt-card { background: #fff; border-radius: 16px; padding: 28px 26px; box-shadow: var(--shadow-2); border-top: 3px solid var(--landscaping-green); }
  .mkt-state { font-family: var(--font-display); font-weight: 700; font-size: 21px; color: var(--freedom-blue); margin: 0; }
  .mkt-tag { font-size: 11px; font-weight: 700; letter-spacing: .14em; text-transform: uppercase; color: var(--landscaping-green); margin: 6px 0 14px; }
  .mkt-cities { font-size: 14px; line-height: 1.75; color: var(--ink-500); margin: 0; }
  .mkts-fl { display: flex; align-items: flex-start; gap: 12px; margin: 24px 0 0; padding: 18px 22px; background: #fff; border-radius: 13px; border-left: 3px solid var(--freedom-blue); max-width: 760px; }
  .mkts-fl p { font-size: 14px; line-height: 1.6; color: var(--ink-500); margin: 0; }
  .mkts-fl strong { color: var(--freedom-blue); font-weight: 600; }

  /* WHY CMGT (condo) */
  .vs { background: var(--freedom-blue); color: #fff; padding: 112px 0; position: relative; overflow: hidden; }
  .vs .leaf { position: absolute; right: -120px; bottom: -150px; width: 480px; height: 480px; opacity: .07; transform: rotate(-12deg); pointer-events: none; }
  .vs .leaf img { width: 100%; filter: brightness(0) invert(1); }
  .vs-in { position: relative; }
  .vs-head { max-width: 740px; margin: 0 0 44px; }
  .vs-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: #fff; margin: 16px 0 14px; text-wrap: balance; }
  .vs-h em { font-style: italic; font-weight: 300; color: var(--landscaping-green); }
  .vs-sub { font-size: 16px; line-height: 1.7; color: rgba(255,255,255,.75); margin: 0; }
  .vs-toggle { display: inline-flex; background: rgba(255,255,255,.08); border: 1px solid rgba(255,255,255,.16); border-radius: 999px; padding: 5px; margin-bottom: 32px; }
  .vs-tbtn { font-family: var(--font-body); font-weight: 600; font-size: 14px; color: rgba(255,255,255,.7); background: transparent; border: 0; border-radius: 999px; padding: 10px 22px; cursor: pointer; transition: all var(--dur-base) var(--ease-out); }
  .vs-tbtn.on { background: var(--landscaping-green); color: var(--freedom-blue); }
  .vs-rows { display: flex; flex-direction: column; gap: 0; max-width: 820px; }
  .vs-rows :global(.vs-row) { display: grid; grid-template-columns: 1fr 1fr; gap: 32px; padding: 24px 0; border-top: 1px solid rgba(255,255,255,.14); }
  .vs-rows :global(.vs-row:last-child) { border-bottom: 1px solid rgba(255,255,255,.14); }
  @media (max-width: 640px) { .vs-rows :global(.vs-row) { grid-template-columns: 1fr; gap: 10px; } }
  .vs-rows :global(.vs-aspect) { font-family: var(--font-display); font-weight: 700; font-size: 17px; color: #fff; }
  .vs-rows :global(.vs-val) { font-size: 14.5px; line-height: 1.6; color: rgba(255,255,255,.78); }
  .vs-rows :global(.vs-val strong) { color: var(--landscaping-green); font-weight: 600; }
  .vs-foot { font-size: 13.5px; color: rgba(255,255,255,.5); margin: 24px 0 0; font-style: italic; }

  /* FULL-BLEED (condo) */
  .fbleed { position: relative; min-height: 480px; display: flex; align-items: center; overflow: hidden; isolation: isolate; }
  .fbleed :global(.image-slot) { position: absolute; inset: 0; width: 100%; height: 100%; z-index: 0; }
  .fbleed::after { content: ""; position: absolute; inset: 0; z-index: 1; background: linear-gradient(100deg, rgba(21,21,92,.92) 0%, rgba(21,21,92,.7) 42%, rgba(21,21,92,.25) 100%); pointer-events: none; }
  .fbleed-in { position: relative; z-index: 2; max-width: 760px; padding: 80px 0; pointer-events: none; }
  .fbleed .eyebrow { color: rgba(255,255,255,.62); }
  .fbleed-q { font-family: var(--font-display); font-weight: 300; font-style: italic; font-size: clamp(30px, 4.4vw, 56px); line-height: 1.16; letter-spacing: -.02em; color: #fff; margin: 18px 0 22px; text-wrap: balance; }
  .fbleed-q .g { font-style: normal; font-weight: 700; color: var(--landscaping-green); }
  .fbleed-sub { font-size: 17px; line-height: 1.65; color: rgba(255,255,255,.8); max-width: 540px; margin: 0; }
  .fbleed-sub strong { color: #fff; font-weight: 600; }

  /* WHAT IT COSTS (condo) */
  .cost { background: #fff; padding: 112px 0; }
  .cost-grid { display: grid; grid-template-columns: .9fr 1.1fr; gap: 64px; align-items: start; }
  @media (max-width: 920px) { .cost-grid { grid-template-columns: 1fr; gap: 36px; } }
  .cost-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 18px; text-wrap: balance; }
  .cost-p { font-size: 16px; line-height: 1.75; color: var(--ink-700); margin: 0 0 16px; max-width: 520px; }
  .cost-p strong { color: var(--freedom-blue); font-weight: 600; }
  .cost-factors { display: flex; flex-direction: column; gap: 10px; }
  .cost-frow { display: flex; gap: 14px; align-items: flex-start; background: var(--nb-lightest); border-radius: 13px; padding: 18px 20px; }
  .cost-fnum { font-family: var(--font-display); font-weight: 300; font-style: italic; font-size: 22px; color: var(--landscaping-green); flex: none; line-height: 1.2; }
  .cost-ft { font-family: var(--font-display); font-weight: 700; font-size: 16.5px; color: var(--freedom-blue); margin: 0 0 5px; }
  .cost-fd { font-size: 14px; line-height: 1.6; color: var(--ink-500); margin: 0; }
  .cost-note { font-size: 13.5px; line-height: 1.6; color: var(--ink-400); margin: 20px 0 0; font-style: italic; }

  /* SWITCHING CALLOUT (condo) */
  .swcall { background: #fff; padding: 100px 0; }
  .swcall-in { background: var(--fb-lightest); border-radius: 18px; padding: 40px 44px; display: flex; align-items: center; justify-content: space-between; gap: 36px; flex-wrap: wrap; position: relative; overflow: hidden; }
  .swcall-in::after { content: ""; position: absolute; right: -50px; bottom: -70px; width: 200px; height: 200px; background: url('/assets/leaf-mark.svg') no-repeat center / contain; opacity: .06; transform: rotate(-12deg); pointer-events: none; filter: brightness(0) saturate(100%) invert(11%) sepia(64%) saturate(4147%) hue-rotate(238deg) brightness(85%) contrast(96%); }
  .swcall-txt { position: relative; max-width: 640px; }
  .swcall-k { font-family: var(--font-body); font-weight: 700; font-size: 12px; letter-spacing: .16em; text-transform: uppercase; color: var(--landscaping-green); }
  .swcall-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(22px, 2.6vw, 30px); line-height: 1.18; letter-spacing: -.015em; color: var(--freedom-blue); margin: 8px 0 8px; text-wrap: balance; }
  .swcall-p { font-size: 15px; line-height: 1.6; color: var(--ink-500); margin: 0; }
  .swcall-cta { position: relative; flex: none; }

  /* BOARD EDUCATION (condo) */
  .edu { background: var(--nb-lightest); padding: 104px 0; }
  .edu-head { max-width: 680px; margin: 0 0 44px; }
  .edu-h { font-family: var(--font-display); font-weight: 700; font-size: clamp(28px, 3.6vw, 44px); line-height: 1.12; letter-spacing: -.015em; color: var(--freedom-blue); margin: 16px 0 14px; text-wrap: balance; }
  .edu-sub { font-size: 16px; line-height: 1.7; color: var(--ink-500); margin: 0; }
  .edu-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 22px; }
  @media (max-width: 880px) { .edu-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 560px) { .edu-grid { grid-template-columns: 1fr; } }
  .edu-card { display: flex; flex-direction: column; background: #fff; border: 1.5px solid var(--hairline-strong); border-radius: 14px; padding: 26px 26px; text-decoration: none; transition: border-color var(--dur-base) var(--ease-out), transform var(--dur-base) var(--ease-out); }
  .edu-card:hover { border-color: var(--landscaping-green); transform: translateY(-3px); box-shadow: var(--shadow-2); }
  .edu-cat { font-size: 11px; font-weight: 700; letter-spacing: .14em; text-transform: uppercase; color: var(--landscaping-green); }
  .edu-t { font-family: var(--font-display); font-weight: 700; font-size: 19px; line-height: 1.22; color: var(--freedom-blue); margin: 10px 0 8px; }
  .edu-d { font-size: 13.5px; line-height: 1.6; color: var(--ink-500); margin: 0 0 16px; flex: 1; }
  .edu-go { display: inline-flex; align-items: center; gap: 7px; font-weight: 600; font-size: 14px; color: var(--freedom-blue); }
  .edu-go .arrow { color: var(--landscaping-green); transition: transform var(--dur-base) var(--ease-out); }
  .edu-card:hover .edu-go .arrow { transform: translateX(4px); }
  .edu-all { display: inline-flex; align-items: center; gap: 8px; font-weight: 600; font-size: 15px; color: var(--freedom-blue); text-decoration: none; margin-top: 32px; border-bottom: 2px solid var(--landscaping-green); padding-bottom: 3px; }
</style>
